meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
  skip: false
run:
  image: null
  command: gunicorn
  args: '["main:app", "--bind", "0.0.0.0:80"]'
  persistenceMount: /data
  containerPort: "80"
